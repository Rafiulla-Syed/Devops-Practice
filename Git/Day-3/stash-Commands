
stash
=====
git stash list
git stash save "label"
git stash show stash@{x}
git stash show -p stash@{x}

POP = Cut
==========
git stash pop
git stash pop stash@{x}

APPLY = Copy
============
git stash apply
git stash apply stash@{x}

DROP = delete
=============
git stash drop
git stash drop stash@{x}


Merge Vs Rebase

Merge -- If we merge the code one branch into the another branch you wil get the merge commit id and all the information related to merged

Rebase --- If we rebase the you can get all the changes without any 'Commit Id"

